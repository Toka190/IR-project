# IR Project: Python-based Information Retrieval System 🔍

This project is a **Python-based Information Retrieval (IR)** system that implements essential IR techniques like **tokenization**, **stemming**, **indexing**, and **phrase query processing**. The system allows you to read text files, process the text, and search for terms or phrases with detailed frequency counts.

---

### 🛠️ **Technologies**:
- **Programming Language**: Python
- **Libraries**: NLTK (Natural Language Toolkit)
- **Techniques Implemented**: Tokenization, Stemming, Indexing, Phrase Query

---

### ✨ **Main Features**:
- **Tokenization**: Splits the text into tokens (words or phrases).
- **Stemming**: Reduces words to their root form (e.g., "running" becomes "run").
- **Indexing**: Builds an index of terms to allow efficient search and retrieval.
- **Phrase Query**: Supports search for exact phrases in the text.
- **Text Preprocessing**: Includes stop word removal and normalization of terms.

---

### 🚀 **How It Works**:
1. **Tokenization**: The system breaks down the text into individual tokens (words or phrases).
2. **Stemming**: It applies stemming algorithms to reduce words to their base or root form.
3. **Indexing**: An index is created to store the frequency of each term in the document.
4. **Phrase Query**: Users can search for an exact phrase, and the system matches it based on frequency counts.

---

### 📝 **Installation**:
1. Clone the repository:
   ```bash
   git clone https://github.com/Toka190/ir-project
   pip install -r requirements.txt
   python ir_project.py
Thanks for visiting my project! Feel free to explore and collaborate! 
